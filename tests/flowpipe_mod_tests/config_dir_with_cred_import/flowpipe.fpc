credential_import "steampipe" {
  source      = "./aws.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_slack" {
  source      = "./slack.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_abuseipdb" {
  source      = "./abuseipdb.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_sendgrid" {
  source      = "./sendgrid.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_virustotal" {
  source      = "./virustotal.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_urlscan" {
  source      = "./urlscan.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_uptimerobot" {
  source      = "./uptimerobot.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_clickup" {
  source      = "./clickup.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_discord" {
  source      = "./discord.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

credential_import "steampipe_pagerduty" {
  source      = "./pagerduty.spc"
  connections = ["*"]
  prefix      = "sp1_" # prefix is optional
} 

