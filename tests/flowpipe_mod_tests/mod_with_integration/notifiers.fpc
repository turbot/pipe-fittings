notifier "admins" {
  notify {
    integration = integration.slack.my_slack_app 
    channel = "#admins"
  }
}

notifier "devs" {
  description = "bar"
  documentation = "abc doc"
  notify {
    integration = integration.slack.my_slack_app 
    channel = "#devs"
  }
  notify {
    integration = integration.webform.default
    to = ["billie", "joe", "mike"]
  }    
}